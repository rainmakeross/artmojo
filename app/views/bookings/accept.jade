extends ../layout

block content
    script(type='text/javascript', src='/javascripts/uiControllers/Booking.js')
        
    include ../elements/header
    //
      / Header
    //
      Container
    .containerr
      .container
        
        .loginContent
          .contentIn
            .ourstoryHeader
              h2
                | Post Quote
                span It’s free & always will be.
            .signupForm
              form#bookingAddFrm( method='post', action='/bookings/doaccept', name='bookingAddFrm', onsubmit='return validateBookingFrm();')
                
                .signupFormRow
                  label Amount:
                  input(type="text", name="amount", id="amount")
                  input(type="hidden", name="id", id="id", value="#{bookingId}")
                  input(type="hidden", name="artistId", id="artistId", value="#{booking.artistId}")
                  input(type="hidden", name="userId", id="userId", value="#{booking.userId}")
                  input(type="hidden", name="userName", id="userName", value="#{booking.userName}")
                  input(type="hidden", name="artistName", id="artistName", value="#{booking.artistName}")
                  span
                    | Admin has all the rights to deactivate the booking without any prior notice to the user. .
                  input(type='submit', value='Post Quote', name='')
                .clr
            .clr
          .clr
    //
      / Container
    include ../elements/footer
    //
      / Footer
